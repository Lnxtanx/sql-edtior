/**
 * elk/constants.ts
 * Node sizing constants and group chrome dimensions for ELK layout calculations.
 */

// ---------------------------------------------------------------------------
// Node size constants
// ---------------------------------------------------------------------------
export const NODE_WIDTH = 250;
export const NODE_HEIGHT_BASE = 60;
export const NODE_HEIGHT_PER_COLUMN = 28;
export const VIEW_NODE_HEIGHT_BASE = 80;
export const VIEW_COL_HEIGHT = 24;

export const FUNCTION_HEIGHT = 90;
export const ENUM_HEIGHT_BASE = 60;
export const ENUM_VAL_HEIGHT = 20;
export const DOMAIN_HEIGHT = 90;
export const ROLE_HEIGHT = 90;
export const SEQUENCE_HEIGHT = 120;
export const EXTENSION_HEIGHT = 90;
export const POLICY_HEIGHT_BASE = 80;
export const POLICY_HEIGHT_PER_ROLE = 20;

// ---------------------------------------------------------------------------
// Group visual chrome
// ---------------------------------------------------------------------------
export const GROUP_HEADER_HEIGHT = 50;  // px reserved for the schema label header
export const GROUP_PADDING_H = 24;      // horizontal padding inside group
export const GROUP_PADDING_B = 20;      // bottom padding inside group
